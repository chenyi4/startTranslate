1.首页的ajax请求了2次，需要只显示一次，另外一个屏蔽显示
判断屏幕大小 require不同的模块(ok)。

2.翻译每一条下面会有一个input框，先去掉，以后再写功能（ok）。

3.文章的chunk不固定的问题，第二次打开的文章地址是变动的问题。

每一篇文章的chunk是每次更新的时候会随机更新掉的，所以json里面的chunk是随机的
所以不能用chunk去标识，只能把每篇文章对应去标识onechunk到数据库里面，
但是目录和文章都是会随机更新的就会出现，这次生成的onechunk,下次可能就不存在了。

a.每篇文章根据目录地址去生成 aaa=>bbb => 名称  生成标识onechunk,。
b.然后读取的时候，就根据地址，来获取onechunk的标识。

c.如果更新文章的名称。
d.需要用原来系统里面的onechunk来循环现有的目录，如果onechunk的值匹配的目录
已经不存在了，那就把该onechunk删除掉。
e.循环目录的地址和现有存在的目录，如果不存在该目录，就去生成一条onechunk
f.该方法需要先触发原来对目录json的更新，再从目录json里面取数据处理

g.生成onechunk，文章获取的时候，用onechunk来获取

本地实现了，需要在线上测试

4.登陆和退出的实现。

5.开发者对文章翻译的可修改，第三行显示翻译。

6.开发者对接口权限的触发，非权限人员不可以使用该接口的功能，return false;